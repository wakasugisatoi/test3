<%= form_with model: [book, book_comment], local: false do |f| %>
 <%= f.text_area :comment, rows: '5', placeholder: "コメントをここに", size: "80", id: "comment_textarea" %>
   <div class="text-right">
    <%= f.submit "送信" %>
   </div>
<% end %>